import 'package:flutter/material.dart';//importa o pacote flutter/material.dart, que é necessário para usar os widgets e recursos do Flutter para construir interfaces de usuário.

// Colors that we use in our app
//definem as cores usadas no aplicativo. Essas constantes são definidas como instâncias da classe Color, que é fornecida pelo Flutter. 
const kPrimaryColor = Color(0xFF0C9869);//
const kTextColor = Color(0xFF3C4046);
const kBackgroundColor = Color(0xFFF9F8FD);

const double kDefaultPadding = 20.0;// define um valor padrão de preenchimento (padding) igual a 20.0. O valor do preenchimento é usado para adicionar espaçamento interno em torno de elementos em um layout.
